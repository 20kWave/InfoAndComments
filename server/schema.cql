CREATE TABLE songs (  
   title VARCHAR,
   artist_name VARCHAR,  
   num_comments NUMBER,
   num_plays NUMBER,
   num_likes NUMBER,
   num_reposts NUMBER,
   release_date TIMESTAMP,
   p_line VARCHAR,
   c_line VARCHAR,  
   PRIMARY KEY (title)  
);

